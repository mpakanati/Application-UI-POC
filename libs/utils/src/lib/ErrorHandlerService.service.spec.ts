// import {ErrorHandlerService} from './ErrorHandlerService.service';
// import { ErrorModel } from '@Tera/widgets/utils/src/models/error.response.model';


describe('ValueService', () => {
    // let service:ErrorHandlerService;
    beforeEach(() => {
        // service = new ErrorHandlerService();
    });
    it('#should Call Error Message', () => {
        expect(1).toBe(1);
        /* let data:ErrorModel;
        let data1:ErrorModel;
        data=<ErrorModel>{
            Value:{
                Message:'Outer Error',
                Status:{
                    Message:'Inner Error'
                }
            }
        }
        data1=<ErrorModel>{
            Value:{
                Status:{
                    Message:'Inner Error'
                }
            }
        }
        let result = service.getErrorMessage(null);
        expect(result).toBe('Some error occured');
        result = service.getErrorMessage(data);
        expect(result).toBe('Outer Error');
        result = service.getErrorMessage(data1);
        expect(result).toBe('Inner Error'); */
  });
});
